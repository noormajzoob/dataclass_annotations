name: datac_annotations
description: annotation utility for data class generator.
version: 0.0.5

repository: https://github.com/noormajzoob/dataclass_annotations

environment:
  sdk: '>=2.19.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: